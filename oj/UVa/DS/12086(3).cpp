# include <cstdio>
# include <cstring>
# include <iostream>
# include <algorithm>

using namespace std;

int log2 (int n)
{
    int ans = 2;
    while (n){
        n = n/2;
        ans++;
    }
    return ans;
}

int m[1009999], a[200005];

int main ()
{
    int t = 0, i, j, k, n, m;

    while (1)
    {
        cin >> n;
        if (n == 0) break;
        if (t) cout << endl;
        ++t;

        for (i =)
    }

    return 0;
}
